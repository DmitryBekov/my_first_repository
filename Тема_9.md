# Тема 9. ООП на Python: концепции, принципы и примеры реализации
Отчет по теме №9 выполнил:
- Беков Дмитрий Александрович
- АИС-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 |  |  |
| Задание 7 | |  |
| Задание 8 |  |  |
| Задание 9 | |  |
| Задание 10 | |  |


## Лабораторная работа №1
### Допустим, что вы решили оригинально и немного странно познакомиться с человеком. Для этого у вас должен быть написан свой класс на Python, который будет проверять, угадал ли человек ваше имя или нет. Создайте класс, указав в свойствах только имя. Дальше создайте функцию __init__(), а в ней сделайте проверку, угадал ли человек ваше имя. Также можете проверить, что будет, если в этой функции указать атрибут, который не указан в вашем классе, например, вызвать фамилию.
```python
class Introduce:
    def __init__(self, name, guess):
        self.name = name
        if guess == self.name:
            print("Вы угадали мое имя!")
        else:
            print("Нет, это не мое имя.")
        
        # Проверка несуществующего атрибута
        try:
            print(self.surname)
        except AttributeError:
            print("Атрибут 'surname' не существует в этом классе.")

# Пример использования
person = Introduce("Михаил", "Иван")
```
### Результат
![Результат выполнения](pic/lab1.png)

## Лабораторная работа №2
### Вам дали задание написать продавцу мороженого программу, которая будет писать, добавили ли топпинг в мороженое, и цену после изменения. Напишите класс, в котором определяется, изменили ли состав мороженого или нет. Реализуйте метод, выводящий «Мороженое с {ТОППИНГ}» в случае добавки, иначе — «Обычное мороженое». Программа должна воспринимать как топпинг только атрибуты типа string.
```python
class IceCream:
    def __init__(self, topping=None, base_price=100):
        self.base_price = base_price
        if isinstance(topping, str):
            self.topping = topping
            self.price = self.base_price + 20
        else:
            self.topping = None
            self.price = self.base_price

    def show_info(self):
        if self.topping:
            print(f"Мороженое с {self.topping}. Цена: {self.price} руб.")
        else:
            print(f"Обычное мороженое. Цена: {self.price} руб.")

# Пример использования
ice = IceCream("шоколад", 80)
ice.show_info()

```
### Результат
![Результат выполнения](pic/lab2.png)

## Лабораторная работа №3
### Реализовать класс с инкапсуляцией, сеттером, геттером и деструктором. После написания продемонстрировать работу всех функций. Также объяснить, почему в примере на скриншоте (из методички) выводится ошибка — вероятно, из-за обращения к приватному атрибуту напрямую (без использования геттера/сеттера).
```python
class Secret:
    def __init__(self, value):
        self.__hidden = value  # инкапсуляция (приватный атрибут)
        print("Объект создан.")

    def get_value(self):
        return self.__hidden

    def set_value(self, new_value):
        self.__hidden = new_value
        print("Значение обновлено.")

    def __del__(self):
        print("Объект удален.")

# Пример использования
obj = Secret("секрет")
print(obj.get_value())
obj.set_value("новый секрет")
print(obj.get_value())

```
### Результат
![Результат выполнения](pic/lab3.png)

## Лабораторная работа №4
### Написать три класса: Млекопитающие, Кошки, Собаки. При помощи наследования объяснить, что кошки и собаки — млекопитающие. Добавить по одному отличительному атрибуту для кошек и собак.
```python
class Mammal:
    def __init__(self, name):
        self.name = name

    def describe(self):
        print(f"{self.name} — млекопитающее.")

class Cat(Mammal):
    def __init__(self, name, color):
        super().__init__(name)
        self.color = color

    def describe(self):
        print(f"{self.name} — кошка цвета {self.color}, она млекопитающее.")

class Dog(Mammal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed

    def describe(self):
        print(f"{self.name} — собака породы {self.breed}, она млекопитающее.")

# Пример использования
cat = Cat("Мурка", "белого")
dog = Dog("Бобик", "овчарка")
cat.describe()
dog.describe()
```
### Результат
![Результат выполнения](pic/lab4.png)

## Лабораторная работа №5
### Реализовать программу с полиморфизмом, показывающую, как на разных языках люди здороваются. Использовать декоратор @staticmethod, так как не нужны параметры self.
```python
class Russian:
    greeting = "Привет"

    @staticmethod
    def say_hello():
        print(f"На русском говорят: {Russian.greeting}")

class English:
    greeting = "Hello"

    @staticmethod
    def say_hello():
        print(f"In English they say: {English.greeting}")

# Демонстрация полиморфизма
for lang in (Russian, English):
    lang.say_hello()

```
### Результат
![Результат выполнения](pic/lab5.png)

## Самостоятельная работа №1
### Вызвать справку по садоводству.

```python
class Gardener:
    @staticmethod
    def knowledge_base():
        print("""
        Справка по садоводству:
        1. Томаты растут постепенно, проходя стадии: отсутствует → цветение → зелёный → красный.
        2. Ухаживайте за растениями, пока все томаты не созреют.
        3. Урожай можно собирать только после полного созревания.
        """)

# Тест №1 — вызов справки
Gardener.knowledge_base()

```
### Результат
![Результат выполнения](pic/sam1.png)

### Выводы
Вызов статического метода knowledge_base() позволяет получить справку по садоводству без создания объекта.

## Самостоятельная работа №2
### Создать объекты TomatoBush и Gardener.

```python
# === Класс Tomato ===
class Tomato:
    states = {0: 'отсутствует', 1: 'цветение', 2: 'зелёный', 3: 'красный'}

    def __init__(self, index):
        self._index = index
        self._state = 0

    def grow(self):
        if self._state < 3:
            self._state += 1
        self.print_state()

    def is_ripe(self):
        return self._state == 3

    def print_state(self):
        print(f"Томат {self._index}: стадия — {Tomato.states[self._state]}")


# === Класс TomatoBush ===
class TomatoBush:
    def __init__(self, num):
        self.tomatoes = [Tomato(index) for index in range(1, num + 1)]

    def grow_all(self):
        print("\nКуст растёт...")
        for tomato in self.tomatoes:
            tomato.grow()


# === Класс Gardener (упрощённая версия для теста роста) ===
class Gardener:
    def __init__(self, name, plant):
        self.name = name
        self._plant = plant

    def work(self):
        print(f"\n{self.name} ухаживает за растением...")
        self._plant.grow_all()
        print(f"{self.name} закончил работу.")


#
bush = TomatoBush(3)           # Куст из трёх томатов
gardener = Gardener("Михаил", bush)
gardener.work()                # Первый уход

```
### Результат
![Результат выполнения](pic/sam2.png)

### Выводы
Создание объектов TomatoBush и Gardener работает. Метод work() садовника корректно вызывает рост всех томатов на кусте.

## Самостоятельная работа №3
### Садовник ухаживает за кустом.

```python
# === Продолжение предыдущего кода ===

class Gardener:
    def __init__(self, name, plant):
        self.name = name
        self._plant = plant

    def work(self):
        print(f"\n{self.name} ухаживает за растением...")
        self._plant.grow_all()
        print(f"{self.name} закончил работу.")

    def harvest(self):
        print(f"\n{self.name} проверяет урожай...")
        if self._plant.all_are_ripe():
            self._plant.give_away_all()
            print(f"{self.name} собрал урожай! 🍅")
        else:
            print("Рано собирать — томаты ещё не все спелые!")


# Добавляем недостающие методы в куст:
class TomatoBush:
    def __init__(self, num):
        self.tomatoes = [Tomato(index) for index in range(1, num + 1)]

    def grow_all(self):
        print("\nКуст растёт...")
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        print("Урожай собран!\n")
        self.tomatoes = []


# === Тест №3 ===
bush = TomatoBush(3)
gardener = Gardener("Михаил", bush)

# Первый уход
gardener.work()

# Попытка собрать урожай (слишком рано)
gardener.harvest()

```
### Результат
![Результат выполнения](pic/sam3.png)

### Выводы
Метод harvest() проверяет спелость томатов. Если они ещё не дозрели, сбор урожая не выполняется. Логика проверки спелости работает правильно.

## Самостоятельная работа №4
### Попробовать собрать урожай, когда томаты ещё не дозрели.

```python

class Gardener:
    def __init__(self, name, plant):
        self.name = name
        self._plant = plant

    def work(self):
        print(f"\n{self.name} ухаживает за растением...")
        self._plant.grow_all()
        print(f"{self.name} закончил работу.")

    def harvest(self):
        print(f"\n{self.name} проверяет урожай...")
        if self._plant.all_are_ripe():
            self._plant.give_away_all()
            print(f"{self.name} собрал урожай! 🍅")
        else:
            print("Рано собирать — томаты ещё не все спелые!")


# Добавляем недостающие методы в куст:
class TomatoBush:
    def __init__(self, num):
        self.tomatoes = [Tomato(index) for index in range(1, num + 1)]

    def grow_all(self):
        print("\nКуст растёт...")
        for tomato in self.tomatoes:
            tomato.grow()

    def all_are_ripe(self):
        return all(tomato.is_ripe() for tomato in self.tomatoes)

    def give_away_all(self):
        print("Урожай собран!\n")
        self.tomatoes = []


bush = TomatoBush(3)
gardener = Gardener("Михаил", bush)

# Первый уход
gardener.work()

# Попытка собрать урожай (слишком рано)
gardener.harvest()
```
### Результат
![Результат выполнения](pic/sam4.png)

### Выводы
Попытка собрать урожай до полной зрелости показывает предупреждение. Программа корректно не позволяет собирать недозрелые томаты.

## Самостоятельная работа №5
### Продолжить ухаживать и собрать урожай, когда они созреют.

```python
# === Тест №4 — Дозревание и успешный сбор урожая ===

# Создаём куст и садовника
bush = TomatoBush(3)
gardener = Gardener("Михаил", bush)

# Уход №1
gardener.work()
gardener.harvest()  # Рано собирать

# Уход №2
gardener.work()
gardener.harvest()  # Ещё не все спелые

# Уход №3
gardener.work()
gardener.harvest()  # Томаты дозрели — урожай собран

```
### Результат
![Результат выполнения](pic/sam5.png)

### Выводы
После нескольких циклов ухода все томаты дозревают, и урожай можно собрать. Программа демонстрирует полный жизненный цикл томатов и взаимодействие с садовником.
